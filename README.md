# live-education-platform

Lightweight scaffold for a live education platform (placeholder files only).

Project structure

- backend/: Node/Express server, models, routes, and socket handler
- frontend/: React app with components, context, hooks, and pages
- docker-compose.yml: development services configuration

Next steps

- Add implementation code for backend and frontend
- Initialize Git and add a `.gitignore`
- Install dependencies and run local development servers

This repository currently contains empty placeholder files to be filled in.
